class Situation {
  constructor(place, character, summary, options, language) {
    this.place = place; 
    this.character = character;
    this.summary = summary;
    this.options = options;
    this.language = language;
  }
}

module.exports = Situation;